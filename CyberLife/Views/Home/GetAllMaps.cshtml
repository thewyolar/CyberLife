@model IList<MapModel>
@{
    Layout = null;
}
<!--<div>
    <button onClick="loadMap()">loading</button>
</div>-->
@for (int i = 0; i < Model.Count; i++)
{
    <div id="@Model[i].Id" class="mapLoading">@Model[i].Name</div>
}